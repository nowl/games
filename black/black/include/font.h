#ifndef __FONT_H__
#define __FONT_H__

#include <SDL_ttf.h>

/*
class Font {
public:
    static Font *Instance();
    static void Destroy();

protected:
    Font();
    ~Font();
    Font(const Font&);
    Font operator=(const Font&);

private:
    static Font *instance_;
};
*/

#endif /* __FONT_H__ */
